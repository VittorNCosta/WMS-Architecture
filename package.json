{
  "name": "wms-onion",
  "version": "0.1.0",
  "private": true,
  "description": "WMS compacto — controle de estoque e movimentações logísticas internas (Onion Architecture)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "tsx watch src/presentation/server.ts",
    "start": "tsx src/presentation/server.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
