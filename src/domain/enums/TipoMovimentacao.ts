/** Tipos de movimentação de estoque registrados para rastreabilidade. */
export enum TipoMovimentacao {
  ENTRADA = 'ENTRADA',
  ARMAZENAGEM = 'ARMAZENAGEM',
  TRANSFERENCIA = 'TRANSFERENCIA',
  SAIDA = 'SAIDA',
  AJUSTE = 'AJUSTE',
}
