/** Stock movement types recorded for traceability. */
export enum MovementType {
  INBOUND = 'INBOUND',
  PUTAWAY = 'PUTAWAY',
  TRANSFER = 'TRANSFER',
  OUTBOUND = 'OUTBOUND',
  ADJUSTMENT = 'ADJUSTMENT',
}
